describe('Testing mail service token endpoint', () => {

    test('basic', () => {
        const numberOne = 10
        const numberTwo = 20
        const numberThree = 30

        expect(numberOne + numberTwo).toBe(numberThree)
    });

    test('basic again', () => {
        expect(3).toBe(3)
    });

})
